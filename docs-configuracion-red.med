# 🌐 Configuración de Red e Infraestructura del Laboratorio SOC

Este documento detalla la arquitectura de red y las soluciones técnicas implementadas para garantizar la visibilidad del tráfico en un entorno virtualizado sobre Apple Silicon (M-series).

## 📋 Direccionamiento IP y Roles
Se ha establecido una red privada para la comunicación segura entre los nodos del laboratorio:

| Dispositivo | Rol | Sistema Operativo | Dirección IP |
| :--- | :--- | :--- | :--- |
| **Kali Linux** | Atacante (Red Team) | Kali 2024.x | `192.168.64.7` |
| **Ubuntu Server** | Colector de Logs (SOC) | Ubuntu 24.04 LTS | `192.168.64.4` |
| **Parrot OS** | Análisis Forense | Parrot Security | `192.168.64.x` |

## 🛠️ Gestión de Eventos y Monitoreo
Debido a las limitaciones de recursos hardware para ejecutar un stack completo de SIEM (Wazuh Indexer), se ha optado por una arquitectura de **Centralización de Logs nativa**:

* **Tecnología:** Protocolo **Syslog** (Rsyslog).
* **Configuración:** El colector (Ubuntu) ha sido configurado para escuchar tráfico en el puerto `514` (UDP/TCP).
* **Agentes:** Los nodos de ataque y análisis reenvían sus logs críticos al colector central para su posterior auditoría.

## 🧠 Resolución de Conflictos (Troubleshooting)

### 1. Incompatibilidad de Arquitectura (ARM64)
Durante el despliegue de herramientas automatizadas, se detectó un error de validación de arquitectura (`aarch64` vs `x86_64`).
* **Solución:** Se realizó una instalación manual mediante repositorios APT y el uso de banderas de ignorado (`-i`) para forzar la compatibilidad con el chip Apple Silicon.

### 2. Optimización de Memoria RAM
El servicio `wazuh-indexer` reportó fallos de inicio (`exit-code`) debido a la alta demanda de recursos (Java Virtual Machine).
* **Decisión Técnica:** Se pivotó hacia una solución basada en **Rsyslog**, priorizando la estabilidad del sistema y demostrando flexibilidad en la arquitectura de seguridad según los recursos disponibles.

---
*Última actualización: 21 de enero de 2026*
